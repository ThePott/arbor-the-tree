import type { None, XsToXl } from "../interfaces"

export const gapToCn: Record<XsToXl | None, string> = {
    none: "gap-0",
    xs: "gap-my-xs",
    sm: "gap-my-xs",
    md: "gap-my-xs",
    lg: "gap-my-xs",
    xl: "gap-my-xs",
}
