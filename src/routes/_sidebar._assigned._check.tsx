import ReviewCheckLayout from "@/features/_sidebar._assigned._check/layout"
import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/_sidebar/_assigned/_check")({
    component: ReviewCheckLayout,
})
